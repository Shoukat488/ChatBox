<?php
    $username = isset($_POST['username'])?$_POST['username']:"";
    
    echo $username ;
    // $username += ".txt";
    // $file = fopen('name.txt','r') or die("Sorry, we can't open file");

    //echo $username;
    // $JsonFile = json_encode($username,true);
    // echo $JsonFile;
    // $username = $JsonFile;
    // echo $username;


